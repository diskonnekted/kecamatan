# Server

PORT=3033
NODE_ENV=production

# Path ke database SQLite (opsional, default .data/portal.db)
DATABASE_PATH=/home/rapidnet-banjarmangu/htdocs/banjarmangu.rapidnet.id/.data/portal.db

# Session secret untuk admin (generate: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))")
SESSION_SECRET=
